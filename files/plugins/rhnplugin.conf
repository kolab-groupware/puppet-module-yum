# Managed with Puppet

[main]
# This plugin is disabled by default, as the interval that Puppet runs with
# and the first 4 (!) package types you declare will cause the node to be
# blacklisted on Red Hat Network
enabled = 0
gpgcheck = 1

[rhel-i386-server-5]
enabled = 1

